<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chuyende.hotelbookingappofhotel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelBookingAppOfHotel">
        <activity android:name=".activities.MainFragment">

        </activity>
        <activity android:name=".activities.ManHinhChiTietHuy" />
        <activity android:name=".activities.ManhinhChiTietThanhToan" />
        <activity android:name=".activities.ManHinhChiTietDat" />
        <activity android:name=".activities.ManHinhDangNhap" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity">

        </activity>
    </application>

</manifest>